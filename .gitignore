node_modules
.DS_Store

# Keep environment variables out of version control
.env
.env.local
.env*.local
# Backup copies of env files (e.g. .env.backup, .env.local.backup) must never be committed
.env*.backup
*.backup

# Generated Prisma client (regenerated by `prisma generate` via postinstall)
/prisma/generated
.next/
tsconfig.tsbuildinfo

.vercel
